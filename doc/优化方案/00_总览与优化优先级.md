# 00 — 总览与优化优先级

> **状态**（2026-09-22 实测更新）：
>
> - **Phase 1**：15/15 已落地，`cargo test --lib` 38 passed / 0 failed（0.57s，连跑 30 轮 0 失败；套件现已随 T3-07 增至 47 passed、随 T2-04 增至 49、随 T4-10 增至 **52 passed / 0 failed，0.52–1.89 s**）、`cargo clippy --all-targets` 0 warning、`npm run build` ✓ 2.25s、`cargo build --release` ✓ 12.30s（T4-06 依赖清理后复跑：build ✓ 2.33s / 1,535.21 kB、release ✓ 19.68s 全量；sysinfo 0.33 升级后复跑 build ✓ / **1,535.75 kB**（gzip 474.65 kB）、release ✓ 14.08s、产物 **14 542 960 B** = 13.87 MB，较 0.30 的 14 517 792 B 增 25 168 B；T4-10 后 build ✓ 2.48s / **1,536.82 kB**（gzip 475.06 kB）、release ✓ 14.61s、产物仍 14 542 960 B；T4-02/T4-03 两步纯前端重构后 build ✓ 2.33s / **1,537.23 kB**（gzip 475.27 kB，合计 +0.41 kB），release 强制重链 ✓ 16.24s 产物仍 14 542 960 B —— 该"不变"对前端改动没有灵敏性（段 16 KiB 对齐），前端体积看 `dist`、内嵌是否换新查 chunk 文件名，见 06；**首次 `npm run tauri build` ✓ 59.90s 量到真实发布物：`.app` 内二进制 strip 后 **13 293 312 B = 12.68 MB**、`.dmg` **7 467 187 B = 7.12 MB**，此前各轮的 13.87 MB 都是未 strip 的 cargo 产物**），并已用真实启动的二进制与 `sysctl`/`vm_stat`/`ifconfig`/`ps` 对账 —— 见 [05 实施记录](./05_实施路线与任务拆解.md#phase-1-实施记录2026-09-22head-7cda8a0--工作区改动) 与 [06 运行时对账](./06_测试验收与风险清单.md#运行时对账2026-09-22-0116cargo-run-debug-版真实启动)
> - **Phase 2**：12/12 已落地（单例采集 + 事件推送 + CPU 双采样 + 进程流 + 动态频率 + 背压 + 内存压力 + **T2-01 sysinfo `0.30.13 → 0.33.1`**〔16 处 API 破坏性变更全适配；刻意停在方案点名的 0.33 而非最新 0.39.6〕 + **T2-04 磁盘读写速率**〔IOKit 块设备计数按挂载点缓存、窗口均值，已与 `iostat` 逐帧对账，见 05〕）
> - **Phase 3**：9/10 已落地（磁盘分区表 T3-01〔读写两列已随 T2-04 补齐〕、网络接口表 T3-02、**进程服务端搜索/排序/分页 T3-03**、**进程详情抽屉 T3-04**、每核心 CPU T3-05、趋势时间范围 T3-06、**历史持久化 T3-07**〔JSON Lines 落盘 + 跨重启回填，release 产物真机两轮实测 12 点 / 相邻 10 026 ms / 0 乱序，见 05〕、**清理前确认对话框 T3-10**、**垃圾扫描进度事件 + 取消 T3-09**）；仅剩 T3-08 启动项操作（要移动/删除用户真实 `~/Library/LaunchAgents` 文件，需明确授权后才动）
> - **Phase 4**：**10/10 全部收口**（**T4-01 `App.tsx` 拆分〔1500 → 585 行，15 个组件文件 + `hooks/usePrefs.ts`/`useProcessQuery.ts`，`Tabs.TabPane` → `items`〕**、**T4-08 每 Tab `ErrorBoundary`**、T4-05 死代码清除、T4-07 偏好持久化〔暗色/频率/趋势范围/当前 Tab，读侧全部过白名单 —— 陌生持久化值曾把界面打成空白页，见 06〕、T4-09 index.html、**T4-06 未使用依赖移除**〔后端 `chrono`/`which`/`getrandom` 实测 0 引用后删除，`Cargo.lock` 474 → 469；前端 `z-biz-tool-shared` 一并移除〕、**T4-10 统一错误处理**〔`AppError.code` → 提示级别与"数据已过期"动作，10 处 catch 收口到 `lib/error_ui.ts`；顺带修掉 `AppError::unsupported` 缺失这一只在第三类平台才暴露的编译错误〕、**T4-02 偏好集中 → `hooks/usePrefs.ts`**、**T4-03 进程查询状态集中 → `hooks/useProcessQuery.ts`**）；**T4-04 记为"决策不接入"**（其通知/主题模块 peer 依赖已删除的 `zustand@^5`，`AppShell`/`ThemeContext` 与刚落地并实测过的 T4-01 布局、T4-07 偏好白名单冲突，理由见 05 追加落地行）。两处口径偏差如实记：T4-02/T4-03 原方案要建 Zustand store，实际改为自定义 hook（唯一消费者是 `App`，引库只会多一条要手工同步的链路）；T4-02 名义范围里的"阈值/Tab 顺序"两项偏好**尚未有产生它们的界面**（阈值等 T5-01 告警引擎），落地时往 `usePrefs` 加字段即被 T5-11 的导入/导出覆盖。**浏览器 console 已 0 条 antd 废弃告警**
> - **Phase 5**：**12/12 全部收口**（分母订正：任务表实为 T5-01…T5-12 十二项，此前误记 /10。**T5-09 跨平台测试矩阵**〔新建 `.github/workflows/ci.yml`：`pull_request` + `workflow_dispatch` 触发（刻意不加 `push`，本仓习惯直接往 main 推 WIP）、`permissions: contents: read`、三条腿 macos-latest / ubuntu-22.04 / windows-latest 共 11 步 `npm ci`→`tsc`→`npm run build`→`cargo test --lib`→`clippy`→Intel 交叉 check；两处是硬约束不是风格：**任何 cargo 步骤都必须排在 `npm run build` 之后**（`dist` 在 `.gitignore` 里，而 tauri-codegen 见 `frontendDist` 目录不存在时直接 panic），以及 clippy **不带 `-D warnings`**（本机只验过 macOS 一侧 0 warning，把没见过的平台 warning 升成失败等于交一条本地查不动的红 CI）；本地实测：`yaml.safe_load` 通过、`npm ci --dry-run` 在临时副本里 exit 0、workflow 命令行原样跑 → **92 passed**/clippy `0 warning`/tsc 0 error；**矩阵自身还没跑过第一次**（要 push 后 `gh workflow run ci.yml`）——顺带量出 Windows 腿只会跑 90/92 条（`netinfo.rs` 两条 `#[cfg(unix)]`），且 `cfg(windows)`/linux 专属分支至今 0 条单测〕、**T5-12 README 按实测重写** —— 逐条对源码核对后改掉 5 处 over-claim，并新增 9 行"⚠️ 已知限制"表；**T5-01 告警引擎**〔新建 `src-tauri/src/alert.rs`：阈值随配置夹取、按 `(指标, 目标)` 维护独立 warning/critical 连续计数、连续 N 帧才触发、同 `(指标, 目标, 级别)` 冷却去抖，判定放在采集循环里、经 `sys://alert` 推送；前端 `hooks/useAlerts.ts` + `components/AlertSettingsDrawer.tsx` 只做下发与展示，阈值偏好经 `usePrefs` 持久化。真机实测：磁盘 98.72 % 连打 3 帧触发 Critical，此后每 60 s 一条（1 s 频率下 3/63/123/183/243 帧），冷却窗口如实生效〕、**T5-03 告警历史落盘 + 趋势图标注**〔T3-07 的 JSON Lines 口径抽成泛型底座 `Jsonl<T>`，告警记录落 `alerts/alerts.jsonl`（5 000 条上限 / 30 天保留 / 修剪基准取文件内最新事件时间而非墙钟）；新增 `get_alert_history` 查询命令，页面截断到 300 条但**不截断窗口真实条数**，前端因此能写"另有 X 条更早的未列"；`target` 只在写盘副本上过 `log_sanitize`，故前端合并时会话副本覆盖落盘副本；趋势图 CPU/内存两图用 `alignAlertMarkers` 画告警竖线 —— 只贴真实采样点、窗口外不画、对齐误差 `maxSkewMs` 必须随图注出；顺带由 clippy 的一条 "field never read" 查出并修掉"整份历史文件读不开时修剪会用空集合覆盖文件"这个真 bug〕）、**T5-11 偏好导入/导出**〔后端新建 `src-tauri/src/prefs.rs`，只管**落盘边界**：`.json` 后缀、目录先 canonicalize 再判、黑名单 + 可写根白名单、64 KiB 上限、`.tmp-<pid>` + `fs::rename` 原子写；前端 `lib/prefs_file.ts` + `components/PrefsTransferModal.tsx` 只管**值语义**（`adopted`/`clamped`/`absent`/`invalid` 四态预览，导入走"预览 → 应用"两步，脏值最多回落默认值、绝不清空界面）。`prefs` 对后端是不透明 JSON —— 合法性口径全项目只有一处。未扩 capabilities（`dialog:default` 自带 `allow-save`/`allow-open`）、未新增 `AppError.code`。真机 IPC：导出 **508 B / `keys=5`** / 5 项偏好逐字段回读一致，`/etc`、缺文件、`.txt` 三条负例分别以 `PATH_DENIED`/`NOT_FOUND`/`INVALID_INPUT` 回来；前端 **13 条**逻辑断言全过。踩到并修掉一个真 bug：macOS 的 `temp_dir()` 是 per-user `$TMPDIR` 而非 `/tmp`，用户在保存框里正常选 `/tmp/x.json` 会被自己拦掉 —— 补的这条豁免**只给偏好读写**，清理删除白名单不引它（多一个可写目录 ≠ 多一个可删目录），见 04 的 SEC-V11〕）、**T5-10 进程列表虚拟滚动**〔一页只装 20 行时"虚拟滚动"是无意义命题，所以先把页容量从写死的 20 放成 `[20, 50, 100, 200, 300]`（上限**刻意等于**后端 `MAX_PROCESSES_PER_PAGE = 300`，不给会被 `normalized()` 静默夹取的选项），再在 `rows.length > 50` 时开 antd 6 自带的 `Table virtual`；后端零改动，分页/排序/过滤仍在 `monitor.rs`。补 `correctPageToRange(total)` 堵上"换大页容量或进程数缩水后旧偏移落到结果集末尾之外 → 后端 `offset.min(total)` 只给空的一页 → 界面像'没进程'"。实测（headless Chrome，500 行数据）：DOM 恒 **15 行**、容器节点 **351**（对照：整页 50 行时节点 **906**）、首帧 **42 ms / 44 ms**（阈值内 25 ms），滚到底 `scrollTop = scrollHeight` 能取到数据末行 `probe-proc-499` 且 DOM 仍 15 行，33 条断言两轮全过；页容量与后端上限的对齐由新增契约测试 `frontend_page_sizes_stay_inside_the_backend_cap` 锁住，并把它 mutate 成 500 验证过会红。测量环境被迫换掉这件事本身记在 06：内置浏览器面板 hidden 时 `requestAnimationFrame` 一次不触发、`setTimeout(50)` 要 446 ms、`ResizeObserver` 不触发，第一轮在它上面量到的"首帧 466 ms"是环境产物、已作废〕、**T5-04/05/06 诊断 Agent（只建议不执行）**〔引擎**没有**按原方案落在 `src/agent/SystemAgent.ts`，而是新建 `src-tauri/src/agent.rs`（1,074 行纯函数 / 25 条单测）—— 本仓无 vitest 基建，只有 Rust 侧能被 `cargo test --lib` 永久拦住，而 T5-06 的四条源码审计必须跑在那个门禁里；前端 `src/agent/AgentPanel.tsx`（230 行）是一个**零 IPC** 组件，提问由 `App.tsx` 下发给 `agent_query`，建议按钮的全部能力就是"切页签"和"把 PID 填进进程表关键字"。安全边界分四层各自有测试把住：`Suggestion` 枚举只有 `OpenTab`/`FocusProcess` 两种导航模板、`Reply.executed` 私有恒 false、JSON 递归 key 审计禁止 command/args/path 出现、三条源码文本审计（分别扫 agent.rs 生产切片 / commands.rs 的 Agent 分节〔自按注释定位，当前 L282–357〕/ 从 `CARGO_MANIFEST_DIR` 读进来的前端 tsx）+ 一条"建议里的 tab 必须是真实页签键"。判定读的是与界面同一份 `latest_metrics()` 快照和同一套告警阈值，不接外部模型、不采命令行参数与环境变量；只有三个排行类意图才付一次性采集的 ~260 ms，且经 `spawn_blocking` 移出 IPC 线程、CPU/内存两榜以 `std::thread::scope` 并发。顺带修掉三处**数据真实性**缺陷（都是读真实引擎输出读出来的）：回显把用户原话转成小写（拆成 `clip` 保大小写回显 + `normalize` 转小写匹配）、网络速率渲染成 `10.5 MB/s/s`（格式化时重复拼 `/s`，现同时断言不含 `/s/s` 且 `/s` 恰好出现 2 次）、内存结论无占比。以及 **H-06：一次性采集路径的每进程 CPU 恒为 0.0** —— 冷 `System` 需要"建表→填基准→留间隔→取差值"三步而代码只有两步，实测向量 `plain_new [0.0, 0.0, 113.2]` vs `narrow [0.0, 105.6, 110.3]`；修法是收口成唯一的 `monitor::collect_processes_warmed`/`collect_top`，并由 `per_process_cpu_is_actually_sampled` 用本机负载钉住。同一轮里把进程流/一次性采集的采样口径统一成窄口径 `process_refresh_kind()`（内存+CPU），不再依赖 `refresh_processes()` 那份由依赖决定的默认口径（`memory+cpu+disk_usage+exe`，会逐进程读可执行路径；查 sysinfo 0.33.1 源码确认里面**没有** `cmd`/`environ`，所以 04 的"不采集命令行参数与环境变量"此前未被违反，但现在才是可证的）——顺带订正了一处把 `everything()` 的行为安到 `refresh_processes()` 头上的注释；抽屉里"工作目录"永久显示 `—` 也一并修掉（详情路径改用 `detail_refresh_kind()` 显式放宽 `exe`+`cwd`）。浏览器 headless Chrome 探针 **11/11 用例**、每条 `ipcCalls == 0`、`consoleErrors == []`，探针文件验完删净；同轮清掉 6 处 antd 6 废弃告警（`Space direction`→`orientation`、`Alert message`→`title`）。**没有**做"Agent 代为执行 + 二次确认弹窗"，也**没有**对话历史/多轮上下文，温度问题在 T5-08 之前如实回答"本机还没有温度/风扇采集通路"而不编数（T5-08 落地后改读真实报告，"不编数"这条口径没变）〕、**T5-07 迷你模式**〔方案原句是"缩小窗口仅显示关键指标"，**窗口刻意没缩** —— `capabilities/default.json` 实测是 8 条**显式**权限、无通配项（逐条列在 04 的 SEC-V02），里面没有任何窗口写类权限（`core:default` 只给只读 getter），JS 侧拿不到 `set_size`/`hide`，而"真缩窗口/原生托盘"分别要扩权限与加 `tray-icon` feature，属于先问再动；于是落地成同窗口紧凑视图：`m` 键或顶栏按钮进、`Esc` 或退出按钮出，浮层开着时不劫持键、焦点在可编辑元素里时也不劫持。新增 `src/components/MiniMenuBar.tsx`（374 行 / 13,179 B）**零 IPC**：只读现有 `snapshot`/`history`/`alertConfig` props，磁盘"看哪一块"复用 `lib/alert.ts` 新增的 `busiestDisk()`（与告警抽屉同一个函数），迷你折线复用 `windowHistory()` + `downsampleHistory()`（与概览趋势图同一条链路），两条 Rust 文本审计把这三点钉死；`miniMode` 是会话级 state，不进 `usePrefs` 那 5 项快照。实测（headless Chrome + CDP 挂真实 App，两轮 **79 条断言全过**，900×600 与 560×420 两档）：进入即 `stop_process_stream`、无首帧时五项读数全 `—` 且折线写"采样点不足"、四个分区里选中 `available` 的 `Data` 89.0 %、网络速率是两接口之和、>3 周期无帧如实报"采集停滞"、推 4 帧只画窗口内 3 点、整轮 IPC 白名单外 0 条、console 0 error。**这一轮顺带撞出并修掉 T5-01 遗留的一条 antd 6 废弃告警**（`InputNumber addonBefore` 只在渲染时打印，之前从未把抽屉挂进带 console 捕获的探针 ⇒ 上一轮"0 条废弃告警"的结论只覆盖了当时渲染过的组件），改成两枚标签后复测清空〕、**T5-08 温度/风扇采集**〔新增 `src-tauri/src/platform/thermal.rs`（909 行 / 22 条单测）。**Linux 真读** `/sys/class/hwmon`（`tempN_input` 毫摄氏度、`fanN_input` 已是 RPM、上限只取硬件自己上报的 `tempN_max`），hwmon 全空才退 `thermal_zone*`（同一颗传感器常两处露）；**macOS/Windows 如实缺测** —— 本机四条免提权通路逐一实测过：`powermetrics --samplers smc` 直接回 `unrecognized sampler: smc`、`sysctl -a | grep -i thermal\|temp` 只有 IPv6 的 tempaddr 键、`pmset -g therm` 三行 `No … recorded`、`ioreg -l -a` 里 30 处 "temperature" 全是类名/方法名（0 个可读数值），只剩私有 SMC（要 root）⇒ 撞 04 的"应用内零 sudo"，于是 `get_thermal` 在本机返回 `{sensors: [], reason: "…SMC…本应用内不执行提权…"}`（这条 JSON 是用临时单测打印 `probe()` 真产物拿到的，不是文案），界面显示 0 行 + 原因，绝不摆一个 0 占位。`probe_at(root)` 把 sysfs 根做成**参数**，所以"只在 Linux 成立的解析"在 mac 上被 12 条建树单测真跑（临时假树 + `Drop` 清理，0 遗留）；判级全项目唯一（`severity_of` 配 `WARN_HEADROOM_C = 10.0`，只对硬件上报的上限起作用），界面与 Agent 都只读后端给的 `severity`。实测：`cargo test --lib` **153 passed / 0 failed / 1.28 s**（HEAD 基线 124 ⇒ **+29**：thermal 22 + agent 6 + commands 接线 1）、clippy **0 warning**、tsc **0 error**、`npm run build` ✓ 4.92 s → **1,581,950 B / gzip 490.21 kB**（+2 239 B）、四条新审计**全部 mutate 验过会红**（含把 SEC-V06 的 sudo 扫描改成递归后，往 `src/platform/thermal.rs` 的**注释**里写 `Command::new("sudo")` 就会被点名 —— 故意只用注释，绝不执行）、GUI 三态各一次干净启动实测且 console **0 error / 0 warning**。**这一轮由实测抓出 3 个真 bug**：① `unsupported_reason` 的 match 键写成 `"darwin"`，而 `std::env::consts::OS` 在苹果上给的是 `"macos"` ⇒ 每台 Mac 的说明都退化成兜底句，旧单测却因为自己喂 `"darwin"` 一直全绿；② 一次失败的重读发 **2** 条 IPC（`thermal === null` 兼任"没查过"，失败本身改了依赖立刻重跑）；③ 0 RPM 的风扇被标成"偏高"（对风扇是反话，现在叫"停转"）。**未验证**：真实 `/sys` 至今 **0 次观测**（本机没有 Linux 运行时，docker daemon 已死、rustup 只有 darwin 两个 target），Windows/macOS 侧温度也仍然 0 读数 —— 后两者是设计选择不是缺陷，前一条是覆盖面问题〕。**T5-02 系统通知**〔用户先前授权的是"可以扩 capabilities"，**实测一条权限都没加**：投递改走插件的 Rust API（JS 侧 `sendNotification()` 返回 `void`，前端根本拿不到结果；而 ACL 只管 webview 能调的插件命令），那份 **8 条显式权限、无通配项**的清单原样保留，并由新增的 `capabilities_stay_the_explicit_eight_without_any_notification_entry` 逐字钉住 —— 哪天改走 JS 通道这条会红。文案口径由源码决定：`tauri-plugin-notification` 桌面端的 `permission_state()` 是恒 `Granted` 的桩、notify-rust 的 macOS 默认后端在句柄 `drop()` 里才发送且 `.ok()` 丢掉错误 ⇒ 界面只说"已提交 N 条 · 失败 M 条"，不许说"已授权/已送达"，`deliveryIsReported` 为 false 时挂那句"真弹没弹请以通知中心为准"。实测：真机跑 debug 产物，磁盘 99.97 % 自然越严重阈值两次，各跟一条 `[notify] Ok(())`（第二条落在第 63 帧 ⇒ 60 s 冷却对通知同样有效）；抽屉三态（读记账失败 / 投递失败 / 成功）各一次，console 0 error / 0 warning；四条新审计全部 mutate 验过会红。**未验**：屏幕上到底有没有弹横幅（本机拿不到，需用户点"发一条测试通知"自行确认）、Linux/Windows 两条腿各 0 次实测投递〕。另记：`npm run build` 的单 chunk 提示不属于任何规划任务（T4-03 是进程 store，方案里没有拆包项），已按现状写进 README 而非含糊带过
>
> 下文的"位置/后果/修复"保留方案撰写时（基线 `7c7e8a2`）的审计事实，P0 条目已补充 ✅ 修复状态。

---

## 项目独特价值

z-biz-tool-sys 定位为**跨平台轻量系统监控仪表盘**（对标 iStat Menus / Stats / htop 理念），在 z-biz-tool 家族中承担"系统层可观测性 + 系统维护操作"职责：

1. **实时资源监控**：CPU（总体 + 每核心）、内存（用量/压力）、磁盘（分区/读写）、网络（上下行/接口）— 后端 `sysinfo` 采集能力已就位
2. **进程管理**：进程列表（PID/名称/CPU/内存/线程）+ 结束进程 — 后端 `get_processes` / `kill_process` 已实现
3. **系统维护**：垃圾文件扫描与清理、大文件查找、启动项管理、DNS 刷新 — 后端 `cleanup.rs` 已实现
4. **趋势可视化**：Recharts 面积图展示最近 60 秒 CPU/内存曲线

技术栈：Tauri 2 (Rust) + React 19 + TypeScript 5 + Ant Design 6 + Recharts 2 + Vite 6 + sysinfo 0.33（2026-09-22 由 0.30.13 升级）

---

## 文档索引

| # | 文档 | 核心内容 |
|---|------|----------|
| 01 | [现状审计与问题清单](./01_现状审计与问题清单.md) | 18 项已确认缺陷 + 4 项待验证假设 + 9 项已存在能力 |
| 02 | [产品能力与交互优化](./02_产品能力与交互优化.md) | 8 大用户流程缺口 + 14 项交互改造排序 |
| 03 | [技术架构与性能优化](./03_技术架构与性能优化.md) | 采集架构重构 + 数据模型 + IPC 契约 + 性能指标 |
| 04 | [数据安全与可靠性](./04_数据安全与可靠性.md) | 危险操作防护 + 权限收敛 + Agent 安全边界 + 日志脱敏 |
| 05 | [实施路线与任务拆解](./05_实施路线与任务拆解.md) | 5 阶段 59 项任务 + 依赖图 + 回滚策略 |
| 06 | [测试验收与风险清单](./06_测试验收与风险清单.md) | 验收矩阵（单元/集成/E2E/安全/性能/故障注入）+ 16 项量化指标 |

---

## 四项核心原则（贯穿所有文档）

| # | 原则 | 当前状态（2026-09-22） | 目标 |
|---|------|--------------------------|------|
| 1 | **危险系统操作需二次确认** | ⚠ kill 已走 `validate_kill` + Modal（critical 需输入进程名）；DNS 刷新有 Popconfirm；清理有确认；启动项操作未实现（按钮已 disabled） | 所有破坏性操作（kill/清理/禁用启动项/DNS 刷新）均需明确确认 + 真实数据 |
| 2 | **只读监控优先** | ✅ 监控链路已接通后端事件流，mock 数据已删除；采集链路只读，操作仍走显式命令 | 监控链路全量接通后端，采集默认只读、低开销 |
| 3 | **采集低开销** | ⚠ 单例采集器 + 1s/3s/10s 分频已落地，自身 CPU 开销尚未量化（P-01 待测） | 单例采集器 + 定时刷新 + 增量推送，自身 CPU < 2% |
| 4 | **Agent 不自动执行破坏性操作** | ✅ T5-04/05/06 已落地：引擎是 `src-tauri/src/agent.rs` 里的纯函数，建议词汇表被 `Suggestion` 枚举封成两种导航模板（切页签 / 在进程表定位 PID），`Reply.executed` 私有恒 false，`src/agent/AgentPanel.tsx` 零 IPC；由 4 条源码/契约审计在 `cargo test --lib` 里钉住（含"像要求代为执行的自由文本一律 refused 且不生成命令文本"）。审计跑在 Rust 侧是因为本仓只有 cargo 有 CI 门禁，TS 侧没有测试框架 —— 详见 04 的 Agent 边界一节。（旧状态：`agent/` 曾是 SQL 死代码、无系统 Agent） | 系统 Agent 仅生成建议/诊断，执行需用户逐条确认 |

---

## 立即行动项（P0）

以下 5 项为**必须立即修复**的安全/功能致命问题：

### P0-1 · 前端监控数据 100% 为 mock，从未调用后端

- **位置**：[App.tsx L112-113](../../src/App.tsx)（`mockSystemInfo` / `generateMockData`）、[L333-338](../../src/App.tsx)（`processData` 硬编码）
- **后果**：CPU/内存/磁盘/网络/进程全部显示假数据；后端 6 个监控命令（`get_system_info` / `get_cpu_usage` / `get_memory_info` / `get_disk_info` / `get_network_stats` / `get_processes`）从未被前端调用
- **修复**：建立 `useSystemMonitor` hook，定时 invoke 后端命令，替换全部 mock 数据源

### P0-2 · 假进程列表 + 真实 kill -9 = 误杀任意进程

- **位置**：[App.tsx L333-338](../../src/App.tsx)（硬编码 PID 1234/2345/3456/4567）→ [L275-282](../../src/App.tsx)（`handleKillProcess`）→ [lib.rs L298-320](../../src-tauri/src/lib.rs)（`kill -9`）
- **后果**：用户点击"结束 Google Chrome"实际执行 `kill -9 2345`，若该 PID 属于其他关键进程则造成系统损害
- **修复**：进程列表必须来自 `get_processes` 真实数据；kill 前校验 PID 存在性与归属

### P0-3 · flush_dns_cache 使用 sudo，GUI 应用内必挂起或失败

- **位置**：[lib.rs L323-355](../../src-tauri/src/lib.rs)（`Command::new("sudo")`）
- **后果**：无 TTY 环境下 sudo 等待密码输入 → 进程挂起；或报 "sudo: a terminal is required"
- **修复**：macOS 改用 `dscacheutil -flushcache` + `killall -HUP mDNSResponder`（无需 sudo）；Linux 改用 `resolvectl flush-caches`；或移除该功能改为提示用户手动执行

### P0-4 · CSP 完全禁用

- **位置**：[tauri.conf.json L23](../../src-tauri/tauri.conf.json)（`"csp": null`）
- **后果**：WebView 可加载任意远程脚本，配合 `shell:allow-execute` 构成完整攻击链
- **修复**：启用严格 CSP（`default-src 'self'; script-src 'self'; connect-src ipc: http://ipc.localhost`）

### P0-5 · 网络接口数据全错（Debug 打印当 IP、MAC 硬编码、收发字节互换）

- **位置**：[lib.rs L121-128](../../src-tauri/src/lib.rs)（`ip: format!("{:?}", data)` / `mac: "AA:BB:CC:DD:EE:FF"`）、[L215-216](../../src-tauri/src/lib.rs)（`bytes_received: total_transmitted()` / `bytes_sent: total_received()` 互换）
- **后果**：网络 Tab 显示 Rust 结构体 Debug 字符串而非 IP；MAC 全为假值；上下行流量颠倒
- **修复**：使用 `sysinfo::Networks` 正确 API 或 `pnet` / `mac_address` crate 获取真实接口信息

---

### P0 修复状态（2026-09-22 实测）

| # | 状态 | 证据 |
|---|------|------|
| P0-1 | ✅ 已消除 | `mockSystemInfo` / `generateMockData` / 硬编码 `processData` 已从 [App.tsx](../../src/App.tsx) 删除；数据源为 `sys://metrics` 事件 + [useSystemMonitor.ts](../../src/hooks/useSystemMonitor.ts)（无事件时回落到 `get_metrics_snapshot` 一次拉取，仍为真实值）；未采集到时显示 `—` |
| P0-2 | ✅ 已消除 | 进程列表来自 `collect_processes()`（真实 PID）；kill 前必经 [safety.rs](../../src-tauri/src/safety.rs) `validate_kill()`：PID≤100、保护名单、属主校验硬拒，执行走 SIGTERM→宽限→SIGKILL；测试 `low_pids_are_blocked` / `terminate_refuses_blocked_targets` |
| P0-3 | ✅ 已消除 | `flush_dns_cache` 改为平台用户态命令并返回 `manualCommand`；测试 `dns_flush_plan_never_escalates_inside_the_app`（决策层：应用内只跑用户态命令，`sudo` 版本只作为给人复制的兜底）+ `dns_failures_offer_a_copyable_command_and_sanitize_the_reason`（失败文案与脱敏）+ `#[ignore]` 的 `dns_flush_returns_within_three_seconds_on_this_machine`（A-04 真机取证 0.010 s） + `backend_never_constructs_a_sudo_command`（扫描全部 `src/*.rs`，`Command::new("sudo")` 计数为 0） |
| P0-4 | ✅ 已消除 | [tauri.conf.json L23](../../src-tauri/tauri.conf.json) `csp` 为 `default-src 'self'; script-src 'self'; …`，capabilities 已移除 `shell:allow-execute` / `fs:allow-read` |
| P0-5 | ✅ 已消除 | [netinfo.rs](../../src-tauri/src/netinfo.rs) 用 `getifaddrs` 取真实 IPv4/IPv6/MAC；速率按接口独立差分。测试 `enumerates_loopback_with_real_addresses` 断言 MAC ≠ `AA:BB:CC:DD:EE:FF`，`network_metrics_carry_real_addresses` 断言每个 IPv4/IPv6 可被 `std::net` 解析 |

---

## 分阶段能力规划

```
Phase 1 (1.5周)   安全加固 + 监控链路接通 — 消除 P0，让仪表盘显示真实数据
    ↓
Phase 2 (1.5周)   采集架构重构 — 单例 System + 定时刷新 + 事件推送 + CPU 采样修正
    ↓
Phase 3 (2周)     产品能力补全 — 进程搜索/排序/详情、磁盘分区、网络per-interface、历史持久化
    ↓
Phase 4 (1.5周)   前端架构治理 — App.tsx 拆分、状态管理、共享库接入、死代码清除
    ↓
Phase 5 (2周)     体验增强 — 告警阈值、菜单栏迷你模式、Agent 诊断助手、跨平台打磨
```

**总周期**：约 8.5 周（1 人全职）或 5 周（2 人并行 Phase 2+4）

---

## 关键数字

| 维度 | 数据 |
|------|------|
| 已确认缺陷 | 18 项（P0×5, P1×8, P2×5）|
| 待验证假设 | 4 项 |
| 已存在能力（不重复建设） | 9 项 |
| 实施任务 | 59 项（5 阶段） |
| 量化验收指标 | 16 项 |
| 测试用例（规划） | 52 项（UT16 + IT10 + E2E8 + SEC8 + PERF6 + FI4） |
| 风险项 | 7 项 |
| 新增模块（规划） | monitor.rs, ipc_contract.ts, useSystemMonitor.ts, ~~useProcessStore.ts~~（实测为 hooks：`useProcessStream`/`usePrefs`/`useProcessQuery`，不引 Zustand）, ~~AlertEngine.ts~~（T5-01 实测落在后端：Rust `src-tauri/src/alert.rs` 承载 `AlertEngine`，前端只加 `hooks/useAlerts.ts`＋`lib/alert.ts`＋`components/AlertSettingsDrawer.tsx`，判定不放前端；T5-03 再加 `components/AlertHistory.tsx`＋`lib/trend.ts`（后者只做"告警时刻 → 真实采样点"的对齐，不产数据）, SystemAgent.ts, MiniMenuBar.tsx |
| **落地进度（2026-09-22 23:08 实测）** | Phase 1 **15/15**、Phase 2 **12/12**、Phase 3 **9/10**（仅 T3-08 待用户授权）、Phase 4 **10/10**（含 T4-04 决策不接入）、Phase 5 **12/12** ⇒ **58/59（98.3%）**。〔**分母订正**：Phase 5 任务表实有 T5-01…T5-12 共 12 项，此前各轮写的 "x/10" 分母是错的（15+12+10+10+12 = 59 才与总数对得上）；Phase 5 已全闭（T5-02 用户授权扩权限，实测一条没加）。**T5-08 温度/风扇已闭**：Linux 真读 `/sys/class/hwmon`，macOS 侧只剩 SMC 私有通路（`powermetrics` 要 root）⇒ 撞"应用内零 sudo"，故按方案口径**如实缺测**（返回 0 行 + 原因，界面与 Agent 都不编数），细节与实测见 [05](./05_实施路线与任务拆解.md)〕。门禁：`cargo test --lib` **216 passed / 0 failed / 1 ignored**（T5-08 一组 **+29** = `platform/thermal.rs` 22 + `agent.rs` 25→31 + `commands.rs` 8→9 接线审计；T5-02 再 **+20** = `notify.rs` 16 + 新增共用的 `contract_fixtures.rs` 4；FI-01~FI-04 补做 **+9**（`monitor.rs` 的注入测试）、IT-01/02/03/07/09 补做 **+7**（并顺手给清理加上「允许根本身不是清理目标」这道第四闸）、UT/SEC 逐条审计补做 **+9**（`ut03/ut04/ut06/ut09/ut11/ut12/sec_t01/sec_t04/sec_t06`，顺带修掉 PID 1 被报成"属于其他用户"的错因）⇒ **198**；基线取 HEAD 的 **124** —— 上一行写的 122 是 `ea253f2`/`0a4084b` 两条提交之前的数，那两条把 Windows 腿的两条缺陷修掉时也带进了测试。此前：120 = `agent.rs` 25 条 + `commands.rs` 2 条 + `monitor.rs` 1 条 H-06 回归；更早 T5-01 +12 告警、T5-03 +12 落盘历史、T5-11 +15 偏好文件、T5-10 +1 前后端页容量契约）、`cargo clippy --all-targets` **0 warning**、`npx tsc --noEmit` **0 error**、`npm run build` ✓ 9.96 s → `dist/assets/index-DpszeWqN.js` **1,584,047 B = 1,584.05 kB / gzip 490.98 kB**（T5-08 收尾是 1,581,950 B / 490.21 kB ⇒ T5-02 净 **+2,097 B / +0.77 kB gzip**；本轮最初基线 `index-zu9E1Kap.js` 1 579 711 B / 489.38 kB ⇒ 两项合计 +4,336 B，CSS 1,441 B 未动）、`npm run tauri build` 的 `.app`/`.dmg` 体积本轮**未重测**（上一轮：二进制 **13 425 248 B**、dmg **7 511 750 B**）；浏览器探针：T5-07 那 **79/79 条断言**（900×600 与 560×420 两档、整轮 IPC 白名单外 0 条、console 0 error/0 warning）之外，T5-08 的温度三态各跑一次干净启动（`initScript` 在应用启动前注入 IPC 桩，不动仓库文件），console **0 error / 0 warning**，表格 5 行读数/上限/状态/颜色逐格核对来自 `probe_at()` 的真产物〕 |

---

## 首要决策

1. **先通后优**：Phase 1 必须先把 mock 数据替换为真实后端数据，否则所有"监控"功能名不副实
2. **安全优先于功能**：P0-2（假 PID 真 kill）和 P0-3（sudo 挂起）在功能开发前必须修复
3. **采集单例化**：废弃"每命令新建 System"模式，改为 Tauri managed state 持有唯一采集器
4. **Agent 只建议不执行**：系统 Agent 可诊断"哪个进程占 CPU 高"，但 kill 操作必须用户手动确认
5. **共享库复用**：接入 `z-biz-tool-shared` 的 AppShell/ThemeContext/notifications，删除 `src/agent/` SQL 死代码
6. **测试跟随实现**：每个 Phase 的任务必须同步编写对应测试，夹具使用 mock 系统数据，不碰真实进程

---

*详细审计*：[01_现状审计与问题清单](./01_现状审计与问题清单.md)
