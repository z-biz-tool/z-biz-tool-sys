# 00 — 总览与优化优先级

> **状态**（2026-09-22 实测更新）：
>
> - **Phase 1**：15/15 已落地，`cargo test --lib` 38 passed / 0 failed（0.57s，连跑 30 轮 0 失败；套件现已随 T3-07 增至 47 passed、随 T2-04 增至 49、随 T4-10 增至 **52 passed / 0 failed，0.52–1.89 s**）、`cargo clippy --all-targets` 0 warning、`npm run build` ✓ 2.25s、`cargo build --release` ✓ 12.30s（T4-06 依赖清理后复跑：build ✓ 2.33s / 1,535.21 kB、release ✓ 19.68s 全量；sysinfo 0.33 升级后复跑 build ✓ / **1,535.75 kB**（gzip 474.65 kB）、release ✓ 14.08s、产物 **14 542 960 B** = 13.87 MB，较 0.30 的 14 517 792 B 增 25 168 B；T4-10 后 build ✓ 2.48s / **1,536.82 kB**（gzip 475.06 kB）、release ✓ 14.61s、产物仍 14 542 960 B；T4-02/T4-03 两步纯前端重构后 build ✓ 2.33s / **1,537.23 kB**（gzip 475.27 kB，合计 +0.41 kB），release 强制重链 ✓ 16.24s 产物仍 14 542 960 B —— 该"不变"对前端改动没有灵敏性（段 16 KiB 对齐），前端体积看 `dist`、内嵌是否换新查 chunk 文件名，见 06；**首次 `npm run tauri build` ✓ 59.90s 量到真实发布物：`.app` 内二进制 strip 后 **13 293 312 B = 12.68 MB**、`.dmg` **7 467 187 B = 7.12 MB**，此前各轮的 13.87 MB 都是未 strip 的 cargo 产物**），并已用真实启动的二进制与 `sysctl`/`vm_stat`/`ifconfig`/`ps` 对账 —— 见 [05 实施记录](./05_实施路线与任务拆解.md#phase-1-实施记录2026-09-22head-7cda8a0--工作区改动) 与 [06 运行时对账](./06_测试验收与风险清单.md#运行时对账2026-09-22-0116cargo-run-debug-版真实启动)
> - **Phase 2**：12/12 已落地（单例采集 + 事件推送 + CPU 双采样 + 进程流 + 动态频率 + 背压 + 内存压力 + **T2-01 sysinfo `0.30.13 → 0.33.1`**〔16 处 API 破坏性变更全适配；刻意停在方案点名的 0.33 而非最新 0.39.6〕 + **T2-04 磁盘读写速率**〔IOKit 块设备计数按挂载点缓存、窗口均值，已与 `iostat` 逐帧对账，见 05〕）
> - **Phase 3**：9/10 已落地（磁盘分区表 T3-01〔读写两列已随 T2-04 补齐〕、网络接口表 T3-02、**进程服务端搜索/排序/分页 T3-03**、**进程详情抽屉 T3-04**、每核心 CPU T3-05、趋势时间范围 T3-06、**历史持久化 T3-07**〔JSON Lines 落盘 + 跨重启回填，release 产物真机两轮实测 12 点 / 相邻 10 026 ms / 0 乱序，见 05〕、**清理前确认对话框 T3-10**、**垃圾扫描进度事件 + 取消 T3-09**）；仅剩 T3-08 启动项操作（要移动/删除用户真实 `~/Library/LaunchAgents` 文件，需明确授权后才动）
> - **Phase 4**：**10/10 全部收口**（**T4-01 `App.tsx` 拆分〔1500 → 585 行，15 个组件文件 + `hooks/usePrefs.ts`/`useProcessQuery.ts`，`Tabs.TabPane` → `items`〕**、**T4-08 每 Tab `ErrorBoundary`**、T4-05 死代码清除、T4-07 偏好持久化〔暗色/频率/趋势范围/当前 Tab，读侧全部过白名单 —— 陌生持久化值曾把界面打成空白页，见 06〕、T4-09 index.html、**T4-06 未使用依赖移除**〔后端 `chrono`/`which`/`getrandom` 实测 0 引用后删除，`Cargo.lock` 474 → 469；前端 `z-biz-tool-shared` 一并移除〕、**T4-10 统一错误处理**〔`AppError.code` → 提示级别与"数据已过期"动作，10 处 catch 收口到 `lib/error_ui.ts`；顺带修掉 `AppError::unsupported` 缺失这一只在第三类平台才暴露的编译错误〕、**T4-02 偏好集中 → `hooks/usePrefs.ts`**、**T4-03 进程查询状态集中 → `hooks/useProcessQuery.ts`**）；**T4-04 记为"决策不接入"**（其通知/主题模块 peer 依赖已删除的 `zustand@^5`，`AppShell`/`ThemeContext` 与刚落地并实测过的 T4-01 布局、T4-07 偏好白名单冲突，理由见 05 追加落地行）。两处口径偏差如实记：T4-02/T4-03 原方案要建 Zustand store，实际改为自定义 hook（唯一消费者是 `App`，引库只会多一条要手工同步的链路）；T4-02 名义范围里的"阈值/Tab 顺序"两项偏好**尚未有产生它们的界面**（阈值等 T5-01 告警引擎），落地时往 `usePrefs` 加字段即被 T5-11 的导入/导出覆盖。**浏览器 console 已 0 条 antd 废弃告警**
> - **Phase 5**：3/10 已落地（**T5-12 README 按实测重写** —— 逐条对源码核对后改掉 5 处 over-claim，并新增 9 行"⚠️ 已知限制"表；**T5-01 告警引擎**〔新建 `src-tauri/src/alert.rs`：阈值随配置夹取、按 `(指标, 目标)` 维护独立 warning/critical 连续计数、连续 N 帧才触发、同 `(指标, 目标, 级别)` 冷却去抖，判定放在采集循环里、经 `sys://alert` 推送；前端 `hooks/useAlerts.ts` + `components/AlertSettingsDrawer.tsx` 只做下发与展示，阈值偏好经 `usePrefs` 持久化。真机实测：磁盘 98.72 % 连打 3 帧触发 Critical，此后每 60 s 一条（1 s 频率下 3/63/123/183/243 帧），冷却窗口如实生效〕、**T5-03 告警历史落盘 + 趋势图标注**〔T3-07 的 JSON Lines 口径抽成泛型底座 `Jsonl<T>`，告警记录落 `alerts/alerts.jsonl`（5 000 条上限 / 30 天保留 / 修剪基准取文件内最新事件时间而非墙钟）；新增 `get_alert_history` 查询命令，页面截断到 300 条但**不截断窗口真实条数**，前端因此能写"另有 X 条更早的未列"；`target` 只在写盘副本上过 `log_sanitize`，故前端合并时会话副本覆盖落盘副本；趋势图 CPU/内存两图用 `alignAlertMarkers` 画告警竖线 —— 只贴真实采样点、窗口外不画、对齐误差 `maxSkewMs` 必须随图注出；顺带由 clippy 的一条 "field never read" 查出并修掉"整份历史文件读不开时修剪会用空集合覆盖文件"这个真 bug〕）。**T5-02 尚未开工且需先确认**：它要引入 `tauri-plugin-notification`，会扩 `capabilities`（当前仅 `core:*`+`dialog:*`），按 04 的口径先问再动。另记：`npm run build` 的单 chunk 提示不属于任何规划任务（T4-03 是进程 store，方案里没有拆包项），已按现状写进 README 而非含糊带过
>
> 下文的"位置/后果/修复"保留方案撰写时（基线 `7c7e8a2`）的审计事实，P0 条目已补充 ✅ 修复状态。

---

## 项目独特价值

z-biz-tool-sys 定位为**跨平台轻量系统监控仪表盘**（对标 iStat Menus / Stats / htop 理念），在 z-biz-tool 家族中承担"系统层可观测性 + 系统维护操作"职责：

1. **实时资源监控**：CPU（总体 + 每核心）、内存（用量/压力）、磁盘（分区/读写）、网络（上下行/接口）— 后端 `sysinfo` 采集能力已就位
2. **进程管理**：进程列表（PID/名称/CPU/内存/线程）+ 结束进程 — 后端 `get_processes` / `kill_process` 已实现
3. **系统维护**：垃圾文件扫描与清理、大文件查找、启动项管理、DNS 刷新 — 后端 `cleanup.rs` 已实现
4. **趋势可视化**：Recharts 面积图展示最近 60 秒 CPU/内存曲线

技术栈：Tauri 2 (Rust) + React 19 + TypeScript 5 + Ant Design 6 + Recharts 2 + Vite 6 + sysinfo 0.33（2026-09-22 由 0.30.13 升级）

---

## 文档索引

| # | 文档 | 核心内容 |
|---|------|----------|
| 01 | [现状审计与问题清单](./01_现状审计与问题清单.md) | 18 项已确认缺陷 + 4 项待验证假设 + 9 项已存在能力 |
| 02 | [产品能力与交互优化](./02_产品能力与交互优化.md) | 8 大用户流程缺口 + 14 项交互改造排序 |
| 03 | [技术架构与性能优化](./03_技术架构与性能优化.md) | 采集架构重构 + 数据模型 + IPC 契约 + 性能指标 |
| 04 | [数据安全与可靠性](./04_数据安全与可靠性.md) | 危险操作防护 + 权限收敛 + Agent 安全边界 + 日志脱敏 |
| 05 | [实施路线与任务拆解](./05_实施路线与任务拆解.md) | 5 阶段 59 项任务 + 依赖图 + 回滚策略 |
| 06 | [测试验收与风险清单](./06_测试验收与风险清单.md) | 验收矩阵（单元/集成/E2E/安全/性能/故障注入）+ 16 项量化指标 |

---

## 四项核心原则（贯穿所有文档）

| # | 原则 | 当前状态（2026-09-22） | 目标 |
|---|------|--------------------------|------|
| 1 | **危险系统操作需二次确认** | ⚠ kill 已走 `validate_kill` + Modal（critical 需输入进程名）；DNS 刷新有 Popconfirm；清理有确认；启动项操作未实现（按钮已 disabled） | 所有破坏性操作（kill/清理/禁用启动项/DNS 刷新）均需明确确认 + 真实数据 |
| 2 | **只读监控优先** | ✅ 监控链路已接通后端事件流，mock 数据已删除；采集链路只读，操作仍走显式命令 | 监控链路全量接通后端，采集默认只读、低开销 |
| 3 | **采集低开销** | ⚠ 单例采集器 + 1s/3s/10s 分频已落地，自身 CPU 开销尚未量化（P-01 待测） | 单例采集器 + 定时刷新 + 增量推送，自身 CPU < 2% |
| 4 | **Agent 不自动执行破坏性操作** | ✗ agent/ 仍为 SQL 死代码（App.tsx 已不引用），无系统 Agent | 系统 Agent 仅生成建议/诊断，执行需用户逐条确认 |

---

## 立即行动项（P0）

以下 5 项为**必须立即修复**的安全/功能致命问题：

### P0-1 · 前端监控数据 100% 为 mock，从未调用后端

- **位置**：[App.tsx L112-113](../../src/App.tsx)（`mockSystemInfo` / `generateMockData`）、[L333-338](../../src/App.tsx)（`processData` 硬编码）
- **后果**：CPU/内存/磁盘/网络/进程全部显示假数据；后端 6 个监控命令（`get_system_info` / `get_cpu_usage` / `get_memory_info` / `get_disk_info` / `get_network_stats` / `get_processes`）从未被前端调用
- **修复**：建立 `useSystemMonitor` hook，定时 invoke 后端命令，替换全部 mock 数据源

### P0-2 · 假进程列表 + 真实 kill -9 = 误杀任意进程

- **位置**：[App.tsx L333-338](../../src/App.tsx)（硬编码 PID 1234/2345/3456/4567）→ [L275-282](../../src/App.tsx)（`handleKillProcess`）→ [lib.rs L298-320](../../src-tauri/src/lib.rs)（`kill -9`）
- **后果**：用户点击"结束 Google Chrome"实际执行 `kill -9 2345`，若该 PID 属于其他关键进程则造成系统损害
- **修复**：进程列表必须来自 `get_processes` 真实数据；kill 前校验 PID 存在性与归属

### P0-3 · flush_dns_cache 使用 sudo，GUI 应用内必挂起或失败

- **位置**：[lib.rs L323-355](../../src-tauri/src/lib.rs)（`Command::new("sudo")`）
- **后果**：无 TTY 环境下 sudo 等待密码输入 → 进程挂起；或报 "sudo: a terminal is required"
- **修复**：macOS 改用 `dscacheutil -flushcache` + `killall -HUP mDNSResponder`（无需 sudo）；Linux 改用 `resolvectl flush-caches`；或移除该功能改为提示用户手动执行

### P0-4 · CSP 完全禁用

- **位置**：[tauri.conf.json L23](../../src-tauri/tauri.conf.json)（`"csp": null`）
- **后果**：WebView 可加载任意远程脚本，配合 `shell:allow-execute` 构成完整攻击链
- **修复**：启用严格 CSP（`default-src 'self'; script-src 'self'; connect-src ipc: http://ipc.localhost`）

### P0-5 · 网络接口数据全错（Debug 打印当 IP、MAC 硬编码、收发字节互换）

- **位置**：[lib.rs L121-128](../../src-tauri/src/lib.rs)（`ip: format!("{:?}", data)` / `mac: "AA:BB:CC:DD:EE:FF"`）、[L215-216](../../src-tauri/src/lib.rs)（`bytes_received: total_transmitted()` / `bytes_sent: total_received()` 互换）
- **后果**：网络 Tab 显示 Rust 结构体 Debug 字符串而非 IP；MAC 全为假值；上下行流量颠倒
- **修复**：使用 `sysinfo::Networks` 正确 API 或 `pnet` / `mac_address` crate 获取真实接口信息

---

### P0 修复状态（2026-09-22 实测）

| # | 状态 | 证据 |
|---|------|------|
| P0-1 | ✅ 已消除 | `mockSystemInfo` / `generateMockData` / 硬编码 `processData` 已从 [App.tsx](../../src/App.tsx) 删除；数据源为 `sys://metrics` 事件 + [useSystemMonitor.ts](../../src/hooks/useSystemMonitor.ts)（无事件时回落到 `get_metrics_snapshot` 一次拉取，仍为真实值）；未采集到时显示 `—` |
| P0-2 | ✅ 已消除 | 进程列表来自 `collect_processes()`（真实 PID）；kill 前必经 [safety.rs](../../src-tauri/src/safety.rs) `validate_kill()`：PID≤100、保护名单、属主校验硬拒，执行走 SIGTERM→宽限→SIGKILL；测试 `low_pids_are_blocked` / `terminate_refuses_blocked_targets` |
| P0-3 | ✅ 已消除 | `flush_dns_cache` 改为平台用户态命令并返回 `manualCommand`；测试 `dns_flush_never_shells_out_to_sudo` + `backend_never_constructs_a_sudo_command`（扫描全部 `src/*.rs`，`Command::new("sudo")` 计数为 0） |
| P0-4 | ✅ 已消除 | [tauri.conf.json L23](../../src-tauri/tauri.conf.json) `csp` 为 `default-src 'self'; script-src 'self'; …`，capabilities 已移除 `shell:allow-execute` / `fs:allow-read` |
| P0-5 | ✅ 已消除 | [netinfo.rs](../../src-tauri/src/netinfo.rs) 用 `getifaddrs` 取真实 IPv4/IPv6/MAC；速率按接口独立差分。测试 `enumerates_loopback_with_real_addresses` 断言 MAC ≠ `AA:BB:CC:DD:EE:FF`，`network_metrics_carry_real_addresses` 断言每个 IPv4/IPv6 可被 `std::net` 解析 |

---

## 分阶段能力规划

```
Phase 1 (1.5周)   安全加固 + 监控链路接通 — 消除 P0，让仪表盘显示真实数据
    ↓
Phase 2 (1.5周)   采集架构重构 — 单例 System + 定时刷新 + 事件推送 + CPU 采样修正
    ↓
Phase 3 (2周)     产品能力补全 — 进程搜索/排序/详情、磁盘分区、网络per-interface、历史持久化
    ↓
Phase 4 (1.5周)   前端架构治理 — App.tsx 拆分、状态管理、共享库接入、死代码清除
    ↓
Phase 5 (2周)     体验增强 — 告警阈值、菜单栏迷你模式、Agent 诊断助手、跨平台打磨
```

**总周期**：约 8.5 周（1 人全职）或 5 周（2 人并行 Phase 2+4）

---

## 关键数字

| 维度 | 数据 |
|------|------|
| 已确认缺陷 | 18 项（P0×5, P1×8, P2×5）|
| 待验证假设 | 4 项 |
| 已存在能力（不重复建设） | 9 项 |
| 实施任务 | 59 项（5 阶段） |
| 量化验收指标 | 16 项 |
| 测试用例（规划） | 52 项（UT16 + IT10 + E2E8 + SEC8 + PERF6 + FI4） |
| 风险项 | 7 项 |
| 新增模块（规划） | monitor.rs, ipc_contract.ts, useSystemMonitor.ts, ~~useProcessStore.ts~~（实测为 hooks：`useProcessStream`/`usePrefs`/`useProcessQuery`，不引 Zustand）, ~~AlertEngine.ts~~（T5-01 实测落在后端：Rust `src-tauri/src/alert.rs` 承载 `AlertEngine`，前端只加 `hooks/useAlerts.ts`＋`lib/alert.ts`＋`components/AlertSettingsDrawer.tsx`，判定不放前端；T5-03 再加 `components/AlertHistory.tsx`＋`lib/trend.ts`（后者只做"告警时刻 → 真实采样点"的对齐，不产数据）, SystemAgent.ts, MiniMenuBar.tsx |
| **落地进度（2026-09-22 16:47 实测）** | Phase 1 **15/15**、Phase 2 **12/12**、Phase 3 **9/10**（仅 T3-08 待用户授权）、Phase 4 **10/10**（含 T4-04 决策不接入）、Phase 5 **3/10** ⇒ **49/59（83.1%）**。门禁：`cargo test --lib` **76/76**（T5-01 新增 12 条告警用例、T5-03 再增 12 条历史/告警落盘用例，`history.rs` 单测 9 → 21）、`cargo clippy --all-targets` 0 warning、`npx tsc --noEmit` 0 error、`npm run build` ✓ **1,553.48 kB / gzip 481.12 kB**、`npm run tauri build` ✓（`.app` 内二进制 **13 377 296 B**、`.dmg` **7 500 047 B**） |

---

## 首要决策

1. **先通后优**：Phase 1 必须先把 mock 数据替换为真实后端数据，否则所有"监控"功能名不副实
2. **安全优先于功能**：P0-2（假 PID 真 kill）和 P0-3（sudo 挂起）在功能开发前必须修复
3. **采集单例化**：废弃"每命令新建 System"模式，改为 Tauri managed state 持有唯一采集器
4. **Agent 只建议不执行**：系统 Agent 可诊断"哪个进程占 CPU 高"，但 kill 操作必须用户手动确认
5. **共享库复用**：接入 `z-biz-tool-shared` 的 AppShell/ThemeContext/notifications，删除 `src/agent/` SQL 死代码
6. **测试跟随实现**：每个 Phase 的任务必须同步编写对应测试，夹具使用 mock 系统数据，不碰真实进程

---

*详细审计*：[01_现状审计与问题清单](./01_现状审计与问题清单.md)
