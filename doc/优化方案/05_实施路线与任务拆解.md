# 05 — 实施路线与任务拆解

> **状态**：设计方案，未实现；基线 `7c7e8a2`；当前事实 = 静态代码证据，指标 = 拟定验收目标
>
> **重要声明**：本文档为规划方案，所有任务均**未执行**。文件路径中"规划新增"表示当前不存在、需创建的文件。

---

## 资源假设

| 角色 | 人数 | 技能要求 |
|------|------|----------|
| 全栈开发（Rust + React） | 1 | 熟悉 Tauri 2 / sysinfo / Zustand / Ant Design |
| 可选：前端开发 | 1 | Phase 4 可并行 |
| 测试 | 开发者自测 + CI | 无独立 QA |

**工时估算基准**：1d = 6h 有效编码时间。总周期 8.5 周（1 人）或 5 周（2 人并行）。

---

## Phase 1 · 安全加固 + 监控链路接通（1.5 周）

**目标**：消除全部 P0，让仪表盘显示真实系统数据。

| 任务 | 描述 | 改造文件 | 前置 | 工时 |
|------|------|----------|------|------|
| T1-01 | 启用 CSP，移除 shell/fs 权限 | [tauri.conf.json](../../src-tauri/tauri.conf.json), [capabilities/default.json](../../src-tauri/capabilities/default.json) | — | 0.5d |
| T1-02 | 移除 tauri-plugin-shell / tauri-plugin-fs 引用 | [Cargo.toml](../../src-tauri/Cargo.toml), [lib.rs L250-252](../../src-tauri/src/lib.rs) | T1-01 | 0.5d |
| T1-03 | 新建 `safety.rs`：KillGuard（PID 校验 + SIGTERM→SIGKILL） | 规划新增: `src-tauri/src/safety.rs` | — | 1d |
| T1-04 | 改造 `kill_process` 命令：接入 KillGuard + 返回 AppError | [lib.rs L298-320](../../src-tauri/src/lib.rs) | T1-03 | 0.5d |
| T1-05 | 修复 `flush_dns_cache`：去 sudo，平台适配 | [lib.rs L323-355](../../src-tauri/src/lib.rs) | — | 0.5d |
| T1-06 | 修复网络接口采集：真实 IP/MAC（引入 `pnet` 或平台 API） | [lib.rs L121-128](../../src-tauri/src/lib.rs), [Cargo.toml](../../src-tauri/Cargo.toml) | — | 1d |
| T1-07 | 修复 `get_network_stats` 收发字节互换 + packets 硬编码 | [lib.rs L215-218](../../src-tauri/src/lib.rs) | T1-06 | 0.5d |
| T1-08 | 新建 `monitor.rs`：MonitorService 单例 + 定时采集 + emit 事件 | 规划新增: `src-tauri/src/monitor.rs` | — | 1.5d |
| T1-09 | 注册 MonitorService 为 Tauri managed state + setup 启动 | [lib.rs L248-269](../../src-tauri/src/lib.rs) | T1-08 | 0.5d |
| T1-10 | 新建前端 `ipc_contract.ts`：MetricsSnapshot / ProcessInfo 类型 | 规划新增: `src/ipc_contract.ts` | T1-08 | 0.5d |
| T1-11 | 新建 `useSystemMonitor` hook：listen 事件 + 环形缓冲 | 规划新增: `src/hooks/useSystemMonitor.ts` | T1-10 | 1d |
| T1-12 | App.tsx 概览 Tab 接通真实数据（替换 mockSystemInfo + generateMockData） | [App.tsx L93-151](../../src/App.tsx) | T1-11 | 1d |
| T1-13 | App.tsx 进程 Tab 接通 `get_processes`（替换硬编码 processData） | [App.tsx L333-338](../../src/App.tsx) | T1-08 | 0.5d |
| T1-14 | kill 前端流程改造：先 validate → 显示真实进程名 → 确认 → 执行 | [App.tsx L275-330](../../src/App.tsx) | T1-04, T1-13 | 0.5d |
| T1-15 | 修复大文件扫描路径展开（后端 `~` → `dirs::home_dir()`） | [cleanup.rs L327-333](../../src-tauri/src/cleanup.rs), [App.tsx L229](../../src/App.tsx) | — | 0.5d |

**Phase 1 回滚策略**：所有改动在独立分支 `fix/phase1-safety`；若 MonitorService 导致崩溃，可回退到 invoke 轮询模式（保留旧命令不删）。

---

## Phase 2 · 采集架构重构（1.5 周）

**目标**：解决 ARCH-01（CPU 采样恒 0）、PERF-01（重复初始化），建立高效采集管线。

| 任务 | 描述 | 改造文件 | 前置 | 工时 |
|------|------|----------|------|------|
| T2-01 | 升级 sysinfo 0.30 → 0.33+（API 适配） | [Cargo.toml](../../src-tauri/Cargo.toml), [lib.rs](../../src-tauri/src/lib.rs), [monitor.rs] | T1-08 | 1d |
| T2-02 | MonitorService 内 System 单例 + Mutex 保护 + delta 计算 | 规划改造: `src-tauri/src/monitor.rs` | T2-01 | 1d |
| T2-03 | CPU 采样修正：保持前次 cpu_times，interval ≥ 200ms | 同上 | T2-02 | 0.5d |
| T2-04 | 磁盘 I/O 速率采集（sysinfo DiskUsage） | 同上 | T2-02 | 0.5d |
| T2-05 | 内存压力等级计算（平台抽象） | 规划新增: `src-tauri/src/platform/mod.rs` | T2-02 | 1d |
| T2-06 | 进程采集独立循环（3s 间隔）+ 排序 + 截断 | 规划改造: `src-tauri/src/monitor.rs` | T2-02 | 0.5d |
| T2-07 | `start_process_stream` / `stop_process_stream` 命令 | [lib.rs](../../src-tauri/src/lib.rs) | T2-06 | 0.5d |
| T2-08 | `set_monitor_config` 命令（动态调整间隔） | [lib.rs](../../src-tauri/src/lib.rs), [monitor.rs] | T2-02 | 0.5d |
| T2-09 | `get_static_info` 命令（启动时一次性） | [lib.rs](../../src-tauri/src/lib.rs) | T2-02 | 0.5d |
| T2-10 | 前端刷新频率选择器 + 调用 set_monitor_config | 规划新增: `src/components/SettingsPanel.tsx` | T2-08 | 0.5d |
| T2-11 | 废弃旧独立命令（get_cpu_usage 等）或改为从缓存读取 | [lib.rs L146-245](../../src-tauri/src/lib.rs) | T2-02 | 0.5d |
| T2-12 | 背压：MissedTickBehavior::Skip + emit 失败静默 | [monitor.rs] | T2-02 | 0.5d |

**Phase 2 回滚策略**：sysinfo 升级若引入兼容问题，Cargo.lock 回退 + monitor.rs 条件编译适配两版 API。

---

## Phase 3 · 产品能力补全（2 周）

**目标**：补全磁盘/网络/进程详情/历史趋势等产品能力。

| 任务 | 描述 | 改造文件 | 前置 | 工时 |
|------|------|----------|------|------|
| T3-01 | 磁盘 Tab：分区列表 + 使用率环 + I/O 速率 | 规划新增: `src/components/DiskTab.tsx` | T2-04 | 1d |
| T3-02 | 网络 Tab 重构：per-interface 实时曲线 + 接口详情 | 规划新增: `src/components/NetworkTab.tsx` | T1-06, T2-02 | 1.5d |
| T3-03 | 进程搜索/过滤/排序（后端 ProcessQuery + 前端 UI） | [monitor.rs], 规划新增: `src/components/ProcessTab.tsx` | T2-06 | 1.5d |
| T3-04 | 进程详情面板（路径/启动时间/父进程） | 同上 | T3-03 | 1d |
| T3-05 | 每核心 CPU 展示（折叠式 mini bar） | 规划新增: `src/components/CpuCores.tsx` | T2-03 | 0.5d |
| T3-06 | 历史趋势：时间范围切换（60s/5min/1h） | [useSystemMonitor.ts] | T1-11 | 1d |
| T3-07 | 历史持久化（SQLite/JSON，10s 采样，7 天保留） | 规划新增: `src-tauri/src/history.rs` | T2-02 | 1.5d |
| T3-08 | 启动项操作实现（disable/remove + 备份 + 确认） | [cleanup.rs], 规划新增: `src-tauri/src/startup.rs` | T1-03 | 1.5d |
| T3-09 | 垃圾扫描进度事件 + 前端进度条 | [cleanup.rs], [App.tsx] | — | 1d |
| T3-10 | 清理前确认对话框（路径列表 + 不可撤销警告） | [App.tsx] | — | 0.5d |

**Phase 3 回滚策略**：新 Tab 组件为独立文件，可逐个移除不影响已有功能。

---

## Phase 4 · 前端架构治理（1.5 周）

**目标**：拆分 App.tsx、接入共享库、清除死代码、建立状态管理。

| 任务 | 描述 | 改造文件 | 前置 | 工时 |
|------|------|----------|------|------|
| T4-01 | App.tsx 拆分为 Tab 组件（Overview/Process/Disk/Network/Cleanup/Startup/System） | [App.tsx](../../src/App.tsx) → `src/components/*.tsx` | T3-01~T3-04 | 1.5d |
| T4-02 | 新建 `settingsStore.ts`（Zustand）：主题/频率/阈值/Tab 顺序 | 规划新增: `src/stores/settingsStore.ts` | — | 0.5d |
| T4-03 | 新建 `useProcessStore.ts`（Zustand）：进程列表 + 查询状态 | 规划新增: `src/stores/useProcessStore.ts` | T3-03 | 0.5d |
| T4-04 | 接入 `z-biz-tool-shared`：AppShell / ThemeContext / notifications | [App.tsx], [package.json](../../package.json) | T4-01 | 1d |
| T4-05 | 删除 `src/agent/` SQL 死代码（5 文件 ~530 行） | [src/agent/](../../src/agent/) | T4-04 | 0.5d |
| T4-06 | 移除未使用依赖或确认 `z-biz-tool-shared` 实际 import | [package.json](../../package.json) | T4-04 | 0.5d |
| T4-07 | 暗色模式持久化（localStorage / shared ThemeContext） | [settingsStore.ts] | T4-02 | 0.5d |
| T4-08 | ErrorBoundary 包裹各 Tab（采集失败不白屏） | 规划新增: `src/components/ErrorBoundary.tsx` | T4-01 | 0.5d |
| T4-09 | index.html 修复（title 本地化 + 移除缺失 vite.svg 引用） | [index.html](../../index.html) | — | 0.5d |
| T4-10 | 统一错误处理：AppError 前端适配（code→UI 行为） | [ipc_contract.ts], 各 Tab | T1-04 | 0.5d |

**Phase 4 回滚策略**：组件拆分为纯重构，保持 props 接口不变；共享库接入失败可回退到自有 ThemeProvider。

---

## Phase 5 · 体验增强（2 周）

**目标**：告警、Agent、迷你模式、跨平台打磨。

| 任务 | 描述 | 改造文件 | 前置 | 工时 |
|------|------|----------|------|------|
| T5-01 | 告警引擎：阈值配置 + 连续超限检测 + 去抖 | 规划新增: `src-tauri/src/alert.rs` | T2-02 | 1.5d |
| T5-02 | 告警通知：tauri-plugin-notification + 应用内 Toast | [Cargo.toml], 规划新增: `src/hooks/useAlerts.ts` | T5-01 | 1d |
| T5-03 | 告警历史 + 趋势图标注 | [history.rs], [AlertHistory.tsx] | T5-01, T3-07 | 1d |
| T5-04 | SystemAgent 重写：自然语言→采集→分析→建议 | 规划新增: `src/agent/SystemAgent.ts` | T4-05 | 2d |
| T5-05 | Agent 面板 UI + 操作建议卡片（确认才执行） | 规划新增: `src/agent/AgentPanel.tsx` | T5-04 | 1d |
| T5-06 | Agent 安全边界：禁止直接调用破坏性命令 | [SystemAgent.ts] | T5-04, T1-03 | 0.5d |
| T5-07 | 迷你菜单栏模式（缩小窗口仅显示关键指标） | 规划新增: `src/components/MiniMenuBar.tsx` | T4-01 | 1.5d |
| T5-08 | 温度/风扇采集（macOS IOKit / Linux hwmon） | 规划新增: `src-tauri/src/platform/thermal.rs` | T2-05 | 1.5d |
| T5-09 | 跨平台测试矩阵（macOS ARM/Intel + Ubuntu 22.04 + Windows 11） | [build.yml](../../.github/workflows/build.yml) | 全部 | 1d |
| T5-10 | 进程列表虚拟滚动（500+ 进程） | [ProcessTab.tsx] | T3-03 | 0.5d |
| T5-11 | 用户偏好导入/导出（JSON 配置文件） | [settingsStore.ts] | T4-02 | 0.5d |
| T5-12 | README 更新：反映真实能力（移除未实现宣称） | [README.md](../../README.md) | 全部 | 0.5d |

**Phase 5 回滚策略**：告警/Agent/迷你模式为独立 feature，可通过 feature flag 或 Tab 隐藏关闭。

---

## 依赖关系图

```
T1-01 ──→ T1-02
T1-03 ──→ T1-04 ──→ T1-14
T1-06 ──→ T1-07
T1-08 ──→ T1-09 ──→ T2-02 ──→ T2-03 ──→ T3-05
  │                    │
  │                    ├──→ T2-04 ──→ T3-01
  │                    ├──→ T2-06 ──→ T2-07 ──→ T3-03 ──→ T3-04
  │                    ├──→ T2-05 ──→ T5-08
  │                    └──→ T3-07 ──→ T5-03
  │
  └──→ T1-10 ──→ T1-11 ──→ T1-12
                              └──→ T3-06

T1-13 ──→ T1-14
T4-01 ──→ T4-04 ──→ T4-05 ──→ T5-04 ──→ T5-05 ──→ T5-06
T4-02 ──→ T4-07
T5-01 ──→ T5-02 ──→ T5-03
```

---

## 兼容性承诺

| 维度 | 策略 |
|------|------|
| 旧版 IPC 命令 | Phase 2 保留 `get_cpu_usage` 等旧命令（改为从缓存读取），Phase 4 标记 deprecated，Phase 5 后移除 |
| 用户数据 | 无持久化数据（当前），Phase 3 引入 history.db 为新增文件，不影响旧版 |
| 配置迁移 | localStorage key 使用 `z-biz-sys:v1:*` 前缀，未来版本迁移脚本 |
| 平台兼容 | 最低支持 macOS 12 / Windows 10 1809 / Ubuntu 20.04（与 Tauri 2 一致） |
| 回滚 | 每 Phase 独立分支 + tag；回滚 = revert merge commit + 重新构建 |

---

## 不执行声明

本文档所有任务均为**规划设计**，未在代码库中执行任何改动。实施时需：
1. 创建 feature 分支
2. 逐任务提交（commit message 引用任务编号）
3. 每 Phase 结束运行完整测试矩阵（见 [06_测试验收与风险清单](./06_测试验收与风险清单.md)）
4. Code Review 后合入 main

---

*下一步*：[06_测试验收与风险清单](./06_测试验收与风险清单.md)
